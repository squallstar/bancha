===========
Theme hooks
===========

A theme can define a great amount of hooks to edit many core features without touching core files.
To enable hooks, a theme must implement a **hooks.php** file in its root directory.